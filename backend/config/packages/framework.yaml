framework:
    secret: '%env(APP_SECRET)%'
    http_method_override: false
    handle_all_throwables: true
    http_client:
        enabled: true
    php_errors:
        log: true

when@test:
    framework:
        test: true
        php_errors:
            log: true
